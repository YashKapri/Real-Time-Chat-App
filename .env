# Backend-side environment variables (used by serverless.yml via env:)
AWS_REGION=ap-south-1
STAGE=dev
JWT_SECRET=dev-secret-change-me

# Optional overrides (normally you let serverless derive names)
FRONTEND_BUCKET_NAME=chat-realtime-app-dev-frontend
UPLOADS_BUCKET_NAME=chat-realtime-app-dev-uploads

# Frontend (Vite) - create frontend/.env with these
# VITE_WS_ENDPOINT=wss://<your-websocket-api-id>.execute-api.ap-south-1.amazonaws.com/dev
# VITE_UPLOAD_API_URL=https://<your-http-api-id>.execute-api.ap-south-1.amazonaws.com/uploads/presign
